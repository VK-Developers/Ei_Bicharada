{
  "name": "hey_bicharada",
  "version": "1.0.0",
  "description": "Hey Bicharada App",
  "main": ".eslintrc.js",
  "scripts": {
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\"",
    "dev": "concurrently \"cd backend && npm run prestart && npm run dev\" \"cd frontend && npm start\"",
    "dev:backend": "cd backend && npm run prestart && npm run dev",
    "compose": "docker-compose down && docker-compose up -d"
  },
  "author": "Vincenzo F. Di Giacomo",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
